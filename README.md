
  # Design Log Tracker App

  This is a code bundle for Design Log Tracker App. The original project is available at https://www.figma.com/design/5ev6EY13k5lPinf9eOou0I/Design-Log-Tracker-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  